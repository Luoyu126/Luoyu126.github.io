<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chenyy.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://chenyy.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-05T09:36:01+00:00</updated><id>https://chenyy.github.io/feed.xml</id><title type="html">Yunyi’s Page</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">从PnP到系统底层：Verl灵活适配RL的设计哲学</title><link href="https://chenyy.github.io/blog/2026/verl-hybrid-controller-design-philosophy/" rel="alternate" type="text/html" title="从PnP到系统底层：Verl灵活适配RL的设计哲学"/><published>2026-05-05T09:10:00+00:00</published><updated>2026-05-05T09:10:00+00:00</updated><id>https://chenyy.github.io/blog/2026/verl-hybrid-controller-design-philosophy</id><content type="html" xml:base="https://chenyy.github.io/blog/2026/verl-hybrid-controller-design-philosophy/"><![CDATA[<h1 id="一前言">一、前言</h1> <p>这篇blog的出发点很直接：我在做后训练任务时长期把 <code class="language-plaintext highlighter-rouge">verl</code> 当成“能跑就行”的训练脚手架，用它拼配置、拉起任务、看 loss 和 reward 曲线，但对它为什么快、快在哪里、为什么它被称为 RL 框架，其实并没有形成系统理解。直到最近在实验中碰到吞吐与时延问题（尤其是 rollout 与 update 的时间关系），我于是产生了去读一读Verl框架的兴趣。在读的过程中顺便补充了一些基础知识，如后训练过程中训推的关键差异、多gpu计算的范式等。有意思的是在整理这些知识的时候发现他们基本都出自我之前学过的讲义，如d2l（动手学深度学习）。如果你也和我一样，是一个在任务中学习更有体验感的人，欢迎你跟随这篇blog的逻辑，以理解Verl框架为出发点去梳理沿途的知识点，最后对Verl框架也有一个PnP之上的理解。</p> <p>在开始读源码之前，我先补了一轮背景调研，主要想回答一个问题：<strong>为什么这些年 RL 后训练框架很多，但 <code class="language-plaintext highlighter-rouge">verl</code> 会这么快成为主流选项之一？</strong></p> <p>要理解 <code class="language-plaintext highlighter-rouge">verl</code> ，必须先拆解分布式系统中的“控制权”逻辑。在 LLM 这种超大规模分布式 Workload 中，如何管理成百上千个 GPU 进程，决定了框架的上限。</p> <ul> <li><strong>Single-Controller (MPMD)</strong>：这是一种典型的“主从模式”。一个中心化的 Python 进程（如主控制脚本）通过 RPC 直接对接集群里的<strong>每一张显卡</strong>。虽然这让开发者能像写单机脚本一样方便，但当卡数增加，主进程的分发压力和 RPC 通信开销会成为系统瓶颈，导致严重的“指挥官迟钝”。</li> <li><strong>Multi-Controller (SPMD)</strong>：为了追求极致效率，许多框架走向了另一个极端——让每张卡都运行完全相同的程序副本。在这种模式下，vLLM 推理或 FSDP 训练后端能发挥出硬件的巅峰性能，但代价是代码变得极其零散且难以编写，就像让成千上万名士兵在没有对讲机的情况下全靠默契协同，开发门槛极高。</li> <li><strong>verl 的 Hybrid-Controller（混合控制）</strong>：<code class="language-plaintext highlighter-rouge">verl</code> 的高明之处在于它选择“全都要”。它在宏观上保留了 <strong>Single-Controller</strong> 的编排逻辑，让用户只需写 <code class="language-plaintext highlighter-rouge">actor.generate()</code> 这样直观的代码；但在微观执行上，它将任务下发给由 <strong>Ray</strong> 启动的一组组“子节点群”——这些群组内部以 <strong>Multi-Controller</strong> 模式高速运行 vLLM 或 FSDP 等专业引擎。</li> </ul> <p>这种“<strong>主进程管引擎群，引擎群管显卡</strong>”的层级结构，既利用了 vLLM 和 FSDP 这种顶级“工人”的垂直能力，又通过异步 RPC 解决了大规模集群的调度延迟。这也正是 <code class="language-plaintext highlighter-rouge">verl</code> 能够在同一个 PPO 循环中，丝滑切换推理与训练两种截然不同负载的底层奥秘。</p> <p>因此，这篇文章不再停留在“怎么用 verl”，而是尝试回答几个更底层的问题：<br/> 1）<code class="language-plaintext highlighter-rouge">verl</code> 到底把什么模块解耦了，为什么要解耦？<br/> 2）它如何同时接上 vLLM（rollout）与 FSDP（update）这两套计算范式？<br/> 3）one-step-off 异步流水线到底改变了什么，是否真的带来可量化的收益？</p> <p>为回答这些问题，我采用了“<strong>源码 + 实验指标</strong>”双线方法：一方面沿 <code class="language-plaintext highlighter-rouge">main_ppo / ray_trainer / engine_workers / checkpoint_engine</code> 追调用链，另一方面结合一次真实 run 的 W&amp;B timing 指标做反事实估算（异步 vs 同步），尽量把“框架设计意图”与“实际运行效果”对齐起来。</p> <p>从结果看，<code class="language-plaintext highlighter-rouge">verl</code> 的价值不只在于接口齐全，而在于它把 RL 后训练中最核心、最昂贵的两段流水线（gen 与 update）做成了可编排、可异构、可重叠的系统。</p> <h1 id="二基础知识">二、基础知识</h1> <h2 id="21-推理-vs-训练">2.1 推理 vs 训练</h2> <p>在 RL 训练的一个循环中，模型需在“生成采样”（rollout）与“参数更新”（update）两个阶段之间快速切换。</p> <h3 id="211-推理阶段rollout利用固定参数进行增量生成">2.1.1 推理阶段（Rollout）：利用固定参数进行增量生成</h3> <ul> <li><strong>计算本质</strong>：此时模型参数（$W_Q, W_K, W_V$ 等权重矩阵）是<strong>固定的</strong>，此阶段要做的是在固定参数的前提下通过自回归采样生成最终的序列并拿到奖励。</li> <li><strong>Token 绑定</strong>： <ul> <li>模型将每个新输入的 Token 通过 $W_K$ 和 $W_V$ 矩阵映射为对应的向量，并将其存入 <strong>KV Cache</strong> 中。</li> <li>每一步生成仅需产出一个新的 $Query$，并去访问（搬运）显存中已有的 $KV$ 序列。</li> </ul> </li> <li><strong>显存负载</strong>：由于不涉及反向传播，系统无需保留中间激活值，显存压力主要源于随序列增长而累积的 <strong>KV Cache</strong>。</li> <li><strong>瓶颈</strong>：由于需要频繁从显存中搬运大量的 $KV$ 数据，系统性能受限于<strong>显存带宽</strong>。</li> </ul> <h3 id="212-训练阶段update根据反馈更新参数矩阵">2.1.2 训练阶段（Update）：根据反馈更新参数矩阵</h3> <ul> <li><strong>计算本质</strong>：此时模型参数是<strong>待优化的变量</strong>，需要根据拿到的奖励/损失进行更新学习。</li> <li><strong>句子绑定</strong>： <ul> <li>系统将 Rollout 产生的完整序列一次性输入重新进行前向传播。通过矩阵乘法（$X \times W$），全序列的 $Q, K, V$ 被一次性并行计算出来。而后加入注意力掩码机制将矩阵右上角置为负无穷模拟序列化生成过程，并根据loss对前序参数的梯度更新参数。</li> </ul> </li> <li><strong>核心负载——激活值驻留</strong>： <ul> <li>为了根据 Loss 计算 $W$ 矩阵的梯度，系统必须在显存中“锁死”前向传播产生的所有中间激活值。</li> <li><strong>更新动作</strong>：根据奖励信号计算 Loss，通过反向传播算出梯度，最终修正 $W$ 矩阵的数值，更新模型参数。</li> </ul> </li> <li><strong>瓶颈</strong>：由于涉及到密集矩阵运算及全量激活值、梯度、优化器状态的存储，系统性能受限于 <strong>GPU 算力峰值</strong>及<strong>显存容量</strong>。</li> </ul> <h2 id="22-多gpu计算范式">2.2 多GPU计算范式</h2> <p>在利用多个GPU进行计算时，目标是利用它们的<strong>计算能力</strong>（算得更快）和<strong>显存空间</strong>（塞下更大的模型）。针对一个小批量（Mini-batch）的训练任务，主要有以下三种拆解方式：</p> <p>1.网络并行（<strong>P</strong>ipeline <strong>P</strong>arallelism）</p> <ul> <li><strong>做法：</strong> 把网络“横着切”。GPU 0 算前几层，GPU 1 算中间几层，以此类推。</li> <li><strong>优点：</strong> 能处理单块 GPU 塞不下的<strong>大模型</strong>（控制每个 GPU 的显存占用）。</li> <li><strong>痛点：</strong> <ul> <li><strong>同步困难</strong>：各层计算量不均会导致有的 GPU 闲死，有的忙死。</li> <li><strong>通信带宽瓶颈</strong>：层与层之间需要传输大量的中间激活值和梯度，容易卡在总线带宽上。</li> </ul> </li> </ul> <p>2.分层并行（<strong>T</strong>ensor <strong>P</strong>arallelism）</p> <ul> <li><strong>做法：</strong> 把每一层内部的计算“拆开”。比如一个有 512 个通道的卷积层，由 2 个 GPU 各负责计算 256 个通道。</li> <li><strong>优点：</strong> 显存随 GPU 数量线性扩展，能支持<strong>更宽</strong>的网络。</li> <li><strong>痛点：</strong> 每一层计算完都需要进行全量同步，通信开销极大，甚至超过了层间拆分。</li> </ul> <p>3.数据并行（<strong>D</strong>ata <strong>P</strong>arallelism）</p> <ul> <li><strong>做法：</strong> 模型不动，把数据“切开”。每个 GPU 都拥有一份完整的模型副本，但处理不同的样本。训练完一个 Batch 后，大家交换并聚合梯度。</li> <li><strong>优点：</strong> <ul> <li><strong>最简单、普适性最强</strong>：几乎适用于所有网络架构。</li> <li><strong>效率高</strong>：同步只发生在 Batch 结束时，且计算和通信可以并行（Overlap）。</li> </ul> </li> <li><strong>痛点：</strong> 无法通过增加 GPU 来训练“更大的模型”（因为每个 GPU 都要装下整个模型）。</li> </ul> <p><img src="/assets/img/blog/blog2/multi-gpu-paradigms.png" alt="多 GPU 计算范式示意图"/></p> <h2 id="23-vllm推理与-fsdp训练">2.3 vLLM（推理）与 FSDP（训练）</h2> <p>为了在 RL 的不同阶段榨干 GPU 性能，<strong>verl</strong> 引入了两个底层逻辑截然不同的引擎后端vLLM和FSDP，这两个引擎分别针对2.1节中提到的推理和训练进行优化。</p> <h3 id="231-vllm推理时的带宽魔术师">2.3.1 vLLM：推理时的“带宽魔术师”</h3> <p>vLLM 是专为提高采样吞吐量设计的纯推理引擎，核心目标是解决推理阶段的访存瓶颈。</p> <p>1.<strong>PagedAttention</strong>：打破显存的“围墙”</p> <p>传统的推理框架（如 HuggingFace Transformers）在存储 KV Cache 时，需要为每个序列预先分配一块<strong>连续</strong>的显存空间。</p> <ul> <li><strong>痛点</strong>：由于生成的文本长度不可预测，系统必须按“最大长度”预留空间。这导致了严重的<strong>内部碎片</strong>（预留了没用上）和<strong>外部碎片</strong>（剩下的零散空间塞不下新序列），显存利用率往往不足 50%。</li> <li><strong>PagedAttention 的解法</strong>：它借鉴了操作系统的虚拟内存逻辑，将 KV Cache 划分为固定大小的“页”（Blocks）。 <ul> <li><strong>非连续存储</strong>：物理上的 KV 向量可以散落在显存的任何角落，通过一张“映射表”与逻辑序列对应。</li> <li><strong>按需动态分配</strong>：只有当新 Token 产生时，才会分配新的物理块。这彻底消除了碎片化，使显存利用率提升至 96% 以上，从而在同等显存下支持极高并发的采样（High Throughput）。</li> </ul> </li> </ul> <p>2.非驻留特性：极致的“阅后即焚”</p> <p>在训练（Update）中，为了计算梯度，模型必须像“录像”一样保留前向传播的所有中间状态。而 vLLM 采样时则采用“直播”模式：</p> <ul> <li><strong>即时销毁</strong>：在处理当前层的算子（矩阵乘法、激活函数等）时，产生的临时张量在完成计算、传递给下一层后，会<strong>立即被显存管理器标记为释放</strong>。</li> <li><strong>空间腾挪</strong>：通过这种非驻留策略，显存中除了模型权重（固定占用）外，几乎所有的剩余空间都被动态分配给了 <strong>KV Cache</strong>。</li> <li><strong>工程意义</strong>：这意味着在 Rollout 阶段，我们可以使用极大的 Batch Size，一次性并行采样成百上千个回复，极大提升了强化学习的数据生产效率。</li> </ul> <p>3.并行策略：<strong>TP</strong>实现低延迟</p> <p>虽然 PagedAttention 提升了吞吐（一次跑很多个），但为了让每个 Token 出来的速度、更快，vLLM 通常配合 <strong>TP</strong> 执行。</p> <ul> <li><strong>原理</strong>：TP 将每一层内部的权重矩阵（如 $W_Q, W_K, W_V$）横向切分到多个 GPU 上。</li> <li><strong>协同作业</strong>：当计算一个 Token 时，所有 GPU 同时启动，各自负责计算结果的一部分，最后通过一次快速的同步（All-Reduce）合并结果。</li> <li><strong>结果</strong>：这种策略避免了网络并行（PP）带来的气泡等待，确保了自回归生成时，单次前向传播的延迟被压制到毫秒级，这对于需要大量采样的 RL 任务至关重要。</li> </ul> <h3 id="232-fsdp训练时的空间管家">2.3.2 FSDP：训练时的“空间管家”</h3> <p>FSDP（Fully Sharded Data Parallelism）是 PyTorch 官方对 <strong>ZeRO-3</strong> 协议的高效实现。它的核心目标是打破单卡显存对模型规模的限制，解决训练阶段的“显存容量受限”瓶颈。FSDP本质上是DP的一种进阶，优化点主要体现在借用TP的思想讲训练状态分布存储在N个GPU上，按需合并调度。</p> <p>1.状态全分片（Sharding）：消除显存冗余</p> <p>在传统的分布式数据并行（DDP）中，每张 GPU 都要完整存储一份模型副本，这造成了极大的空间浪费。FSDP 通过“分而治之”的思想，将三类数据进行了彻底切分：</p> <ul> <li><strong>模型参数（Parameters）</strong>：将原本巨大的权重矩阵切成 $N$ 份（$N$ 为 GPU 数量），每张卡只负责维护其中的 $1/N$。</li> <li><strong>梯度（Gradients）</strong>：反向传播计算出的梯度在同步后不再保留全局副本，仅保留本卡对应分片。</li> <li><strong>优化器状态（Optimizer States）</strong>：这是显存消耗的“隐形杀手”。以 Adam 优化器为例，它需要为每个参数存储一阶和二阶动量。通过分片存储，显存占用直接缩减为原来的 $1/N$。</li> </ul> <p>2.通信换空间：动态的“拼图”计算</p> <p>FSDP 并不是静态的切分，而是一个高度动态的计算流。它通过增加通信开销，换取了训练超大规模模型的能力：</p> <ul> <li><strong>前向传播（Forward）</strong>：当 GPU 0 需要计算第 5 层网络时，它会发起一个 <strong>All-Gather</strong> 操作，瞬间从其他 GPU 那里把第 5 层的参数分片拉过来，拼成一个临时完整的矩阵进行计算。计算一结束，为了给接下来的激活值腾位置，这个临时矩阵会被<strong>立即从显存中抹除</strong>。</li> <li><strong>反向传播（Backward）</strong>：同样的逻辑。系统再次动态拉取参数分片以计算梯度。在梯度产出后，通过 <strong>Reduce-Scatter</strong> 操作将梯度同步并切碎分发到各卡上，随后原始梯度也被销毁。</li> </ul> <p>3.核心意义：为“激活值”腾出战场</p> <p>在 2.1.2 中我们提到，训练阶段最沉重的负担是<strong>激活值驻留</strong>。FSDP 的精髓就在于此：</p> <ul> <li><strong>空间让位</strong>：由于模型参数、梯度、优化器状态都被切碎到了整个集群中，单张 GPU 的基础显存占用（Static Memory）被压到了极低。</li> <li><strong>激活值容纳</strong>：腾出来的这些巨量空间，被用来存放前向传播中产生的所有中间变量。这使得我们可以在不 OOM 的前提下，使用更长的 Sequence Length（如 8k, 32k）或更大的 Batch Size。</li> <li><strong>verl 的必然选择</strong>：在 RL 的 Update 阶段，由于 PPO 等算法需要多次迭代更新，对显存的压榨到了极致。FSDP 这种“通信换空间”的特性，是 verl 能够承载 70B 甚至更大模型进行 RL 训练的底气。</li> </ul> <h3 id="233-verl-的-hybrid-engine权重的变形金刚">2.3.3 verl 的 Hybrid Engine：权重的“变形金刚”</h3> <p><strong>verl</strong> 的核心创新在于 <strong>Hybrid Engine</strong>，它实现了模型权重的动态转换逻辑：</p> <ol> <li><strong>在 Rollout 阶段</strong>：模型切换为 <strong>vLLM</strong> 模式，权重被聚合为 <strong>TP 格式</strong>，利用 PagedAttention 进行高速采样。</li> <li><strong>在 Update 阶段</strong>：KV Cache 清空，权重立刻重新切碎为<strong>FSDP 格式</strong>。显存全力支持激活值驻留，完成参数更新。</li> </ol> <table> <thead> <tr> <th style="text-align: left">维度</th> <th style="text-align: left">vLLM (Rollout)</th> <th style="text-align: left">FSDP (Update)</th> </tr> </thead> <tbody> <tr> <td style="text-align: left"><strong>主要目标</strong></td> <td style="text-align: left">提高采样吞吐，降低延迟</td> <td style="text-align: left">承载大规模参数与激活值训练</td> </tr> <tr> <td style="text-align: left"><strong>分布式策略</strong></td> <td style="text-align: left">通常为 <strong>TP</strong></td> <td style="text-align: left"><strong>FSDP / ZeRO-3</strong></td> </tr> <tr> <td style="text-align: left"><strong>显存杀手</strong></td> <td style="text-align: left"><strong>KV Cache</strong></td> <td style="text-align: left"><strong>Activations</strong></td> </tr> <tr> <td style="text-align: left"><strong>中间状态</strong></td> <td style="text-align: left">立即释放 (Forward Only)</td> <td style="text-align: left">严格保留、按需调度(Forward + Backward)</td> </tr> </tbody> </table> <h2 id="24-分布式异步计算编排ray">2.4 分布式异步计算编排：Ray</h2> <p>在 verl 的系统架构中，RL中多种算法如PPO涉及多个角色（Actor, Critic, Reward, Reference）的频繁交互，且每个角色对计算资源（如算力与显存）及并行模式（如 TP 与 FSDP）的需求高度异构。<strong>Ray</strong> 作为底层分布式执行框架，提供了高效的资源抽象与跨节点协调能力。</p> <h3 id="241-异构资源解耦与-placement-group打破显存互踢">2.4.1 异构资源解耦与 Placement Group：打破“显存互踢”</h3> <p>在复杂的 RL 系统中，最棘手的挑战在于如何处理<strong>计算异构性</strong>：Actor 往往需要低延迟的推理引擎（vLLM + TP 并行），而 Critic 或 Reward Model 则需要高吞吐的训练后端（FSDP/ZeRO-3 分片）。如果简单地将它们堆叠在物理显卡上，由于不同引擎的显存管理策略（如 vLLM 的预分配机制与 FSDP 的动态申请）互不感知，极易引发“显存互踢”导致的 OOM。</p> <ul> <li><strong>逻辑孤岛（Placement Group）</strong>：Ray 引入了 <code class="language-plaintext highlighter-rouge">Placement Group</code> 机制，允许 Verl 在物理集群中划定一组<strong>原子化的逻辑资源池</strong>。它像是在繁忙的机房里圈出了几个“包间”，确保 Actor 的 TP 组和 Critic 的 FSDP 组拥有各自独立的算力与显存配额。</li> <li><strong>策略隔离与独占</strong>：通过这种机制，Verl 可以精确地指定：Actor 独占前 $N$ 张卡的计算核心用于生成，而 Critic 在剩余的 $M$ 张卡上运行状态分片。这种<strong>空间上的物理隔离</strong>，使得 Actor 可以在不被 Critic 抢占显存的前提下，将剩余空间全部腾给 KV Cache。</li> <li><strong>切换的物理前提</strong>：这种稳定的资源边界，是实现 <strong>Hybrid Engine</strong> 动态切换的物理基石。正因为每个 Worker Group 的资源是确定的，Verl 才能丝滑地执行“清空 KV Cache -&gt; 重新分片权重 -&gt; 启动训练”这一系列精密的显存体操，而不用担心受到其他组件的随机干扰。 <h3 id="242-基于分布式多进程-actor-的异步-rpc-通信">2.4.2 基于分布式多进程 Actor 的异步 RPC 通信</h3> </li> </ul> <p>在底层实现上，Ray 的 Actor 机制可以被视为一种高度工程化的<strong>分布式多进程范式</strong>。这种设计彻底解决了传统 Python 并发在 RL 大规模计算中的效率瓶颈。</p> <ol> <li> <p><strong>多进程绕过 GIL</strong></p> <ul> <li><strong>真并行执行</strong>：Ray 通过将模型实例（如 Actor, Critic）封装为 <code class="language-plaintext highlighter-rouge">Remote Actor</code>，本质上是在集群中开启了多个<strong>完全独立的 Python 进程</strong>。由于每个 Actor 运行在独立的进程空间中，拥有各自的 GIL。这使得 <code class="language-plaintext highlighter-rouge">verl</code> 可以让推理进程（vLLM）和训练进程（FSDP）在同一物理节点或跨节点同时运行，互不干扰，从而实现了物理层面的“真并行”。</li> </ul> </li> </ol> <blockquote> <p>关于线程、协程、进程的讨论欢迎参见 <a href="/blog/2026/gil-cpu-parallelism/">这篇 GIL 博客</a></p> </blockquote> <ol> <li><strong>状态化进程驻留</strong> <ul> <li><strong>持久化内存</strong>：不同于普通的临时子进程，Ray Actor 进程在创建后会<strong>持续驻留</strong>在内存中。它长期持有模型权重矩阵（$W$），避免了在 PPO 循环中反复创建进程、重复加载模型带来的巨大初始化开销。</li> <li><strong>微服务化抽象</strong>：这些驻留进程被提升为“分布式微服务”，Trainer 仅需通过进程标识符即可发送指令。</li> </ul> </li> <li><strong>异步非阻塞 RPC：流水线式进程协同</strong> <ul> <li><strong>异步语义</strong>：在 <code class="language-plaintext highlighter-rouge">ray_trainer.py</code> 中，主控进程调用 <code class="language-plaintext highlighter-rouge">remote()</code> 方法后会立即获得一个 <code class="language-plaintext highlighter-rouge">ObjectRef</code>（类似 Future/Promise 凭据），而无需等待目标进程执行完毕。</li> <li><strong>利用率最大化</strong>：这种异步 RPC 机制允许 Trainer 在<strong>采样进程</strong>（Rollout）进行密集矩阵运算的同时，同步触发<strong>奖励进程</strong>（Reward Model）的打分预处理，或者<strong>评估进程</strong>的并发校验。</li> <li><strong>工程意义</strong>：通过这种多进程间的异步协同，<code class="language-plaintext highlighter-rouge">verl</code> 构建起了一套极其高效的计算流水线，最大化了集群硬件的整体利用率，避免了 CPU 调度死锁或单进程阻塞导致的 GPU 闲置。</li> </ul> </li> </ol> <h3 id="243-分布式对象存储与零拷贝传输">2.4.3 分布式对象存储与零拷贝传输</h3> <p>RL 训练伴随着海量的中间数据交换（如Rollout阶段产生的输出移交给Update阶段作为输入）</p> <ul> <li><strong>Plasma Object Store</strong>：Ray 维护了一个全局分布式的共享内存存储系统。</li> <li><strong>零拷贝（Zero-copy）效率</strong>：当采样数据在不同的分布式节点（如从 Actor 传输到 Critic）流动时，Ray 利用<strong>共享内存技术</strong>避免了耗时的序列化与反向序列化操作，极大地降低了 PPO 循环中由于数据搬运产生的 IO 延迟。</li> </ul> <h1 id="三verl源码解析">三、Verl源码解析</h1> <p>如第二节中提到，<code class="language-plaintext highlighter-rouge">verl</code> 的系统设计很完整，但这篇文章我主要聚焦两点：第一点是 <code class="language-plaintext highlighter-rouge">verl</code> 最鲜明、也是官方文档中反复强调的核心特征——Hybrid-Controller；第二点是我在实际训练里直接遇到的关键机制——one-step-off-policy。下面的源码解析就围绕这两条主线展开。至于其余实现细节，这里不做展开，仅在第二节保留方法论层面的介绍。</p> <h2 id="31-hybrid-controller-在-verl-里的真实落地">3.1 Hybrid-Controller 在 <code class="language-plaintext highlighter-rouge">verl</code> 里的真实落地</h2> <blockquote> <p>问题：前言里提到的 Hybrid-Controller 在 <code class="language-plaintext highlighter-rouge">verl</code> 里到底怎么实现？这里的 controller 是不是 vLLM/FSDP 这样的推理/训练引擎？</p> </blockquote> <p>先给结论：<strong>不是。</strong><br/> 在 <code class="language-plaintext highlighter-rouge">verl</code> 里，controller 是控制编排层，vLLM/FSDP/Megatron 是执行引擎层。两者是上下层关系，不是同一个概念。</p> <h3 id="311-核心概念">3.1.1 核心概念</h3> <ol> <li> <p><strong>Single-Controller（上层控制）</strong><br/> 可以理解为高层单控制（MPMD）：一个单进程 <code class="language-plaintext highlighter-rouge">RLTrainer</code> 管全局计算图编排，负责 rollout 触发、reward 评分触发、分布式训练任务下发等宏观调度。这个层面关注的是“<strong>先后顺序与数据流</strong>”。</p> </li> <li> <p><strong>Multi-Controller（下层执行）</strong><br/> 可以理解为内部多控制（SPMD）：内部 worker 执行同构程序，用 FSDP/Megatron/VeOmni 等 trainer backend，或 vLLM/SGLang/TensorRT-LLM 等 rollout executor 做重计算，并通过 collectives 同步。这个层面关注的是“<strong>怎么并行算得快</strong>”。</p> </li> <li> <p><strong>Hybrid-Controller（<code class="language-plaintext highlighter-rouge">verl</code> 方案）</strong><br/> 上层保留 single-controller 的灵活编排，下层交给 multi-process/multi-worker 的执行体系。即<strong>主控负责调度 DAG，子节点负责吞吐。</strong></p> </li> </ol> <p><img src="/assets/img/blog/blog2/verl-hybrid-controller-codepath.png" alt="Verl Hybrid-Controller 源码路径示意图"/></p> <h3 id="312-源码里谁是主控">3.1.2 源码里谁是“主控”</h3> <p>这里我用一句最直白的话概括：<strong>主控调 group，就是 single-controller。</strong></p> <p>在代码里，这个“主控”对应 <code class="language-plaintext highlighter-rouge">main_ppo.py</code> + <code class="language-plaintext highlighter-rouge">ray_trainer.py</code> 这一层：</p> <ul> <li><code class="language-plaintext highlighter-rouge">run_ppo</code> 启动 <code class="language-plaintext highlighter-rouge">TaskRunner</code>；</li> <li><code class="language-plaintext highlighter-rouge">TaskRunner.run(...)</code> 创建 <code class="language-plaintext highlighter-rouge">RayPPOTrainer</code>；</li> <li><code class="language-plaintext highlighter-rouge">RayPPOTrainer.fit()</code> 执行主训练循环。</li> </ul> <p>所以主控视角下看到的是这种“单进程风格”调用：</p> <ul> <li><code class="language-plaintext highlighter-rouge">self.async_rollout_manager.generate_sequences(...)</code></li> <li><code class="language-plaintext highlighter-rouge">self.actor_rollout_wg.update_actor(...)</code></li> <li><code class="language-plaintext highlighter-rouge">self.checkpoint_manager.update_weights(...)</code></li> </ul> <p>主控并不直接操作每个 worker，它只表达“这一步该谁算”。</p> <h3 id="313-主控如何通过-rpc-驱动子节点">3.1.3 主控如何通过 RPC 驱动子节点</h3> <p>这一层也可以用一句话概括：<strong>group 自动把任务分发到各个 worker，就是 multi-controller。</strong></p> <p>具体过程是：</p> <ol> <li>每个 worker 先定义自己的方法（如 <code class="language-plaintext highlighter-rouge">update_actor</code>）。</li> <li>这些方法通过 <code class="language-plaintext highlighter-rouge">@register</code> 声明分发/收集规则。</li> <li><code class="language-plaintext highlighter-rouge">RayWorkerGroup</code> 初始化时，把这些 worker 方法绑定成 group 的同名代理方法。</li> <li>当主控调用 <code class="language-plaintext highlighter-rouge">group.update_actor(batch)</code> 时，group 内部自动完成： <ul> <li>数据切分（dispatch）</li> <li>对每个 worker 的 <code class="language-plaintext highlighter-rouge">method.remote(...)</code> 调用</li> <li>结果聚合（collect）</li> </ul> </li> </ol> <p>这就是前面我们讨论的核心：主控不手写 <code class="language-plaintext highlighter-rouge">for worker in workers</code>，而是通过 group 一次调用完成整组 worker 的 RPC 执行。</p> <h3 id="314-子节点里才是-vllm--fsdp-等引擎">3.1.4 子节点里才是 vLLM / FSDP 等引擎</h3> <p>再强调一次边界：</p> <ul> <li>训练侧通过 <code class="language-plaintext highlighter-rouge">EngineRegistry.new(... backend=strategy ...)</code> 选择 FSDP / Megatron 等；</li> <li>rollout 侧通过 <code class="language-plaintext highlighter-rouge">get_rollout_class(...)</code> 选择 vLLM / SGLang / HF rollout。</li> </ul> <p>这说明：</p> <ul> <li><strong>controller 决定“调用谁、何时调用、数据怎么分发收集”</strong>；</li> <li><strong>engine 决定“这个节点内部用什么并行与 kernel 去算”</strong>。</li> </ul> <p>它们是控制层与执行层的解耦关系，这正是 Hybrid-Controller 的核心含义。</p> <h2 id="32-ppo中actor和vllm的权重同步">3.2 PPO中Actor和vLLM的权重同步</h2> <blockquote> <p>问题：在 <code class="language-plaintext highlighter-rouge">verl</code> 的 PPO 训练中，Actor 更新完参数后，权重是如何同步到 vLLM 的？同步频率是不是每个 step 一次？推理参数是否会“落后一个 step”？</p> </blockquote> <p>这部分可以按两种执行范式来理解：<strong>同步（sync）</strong>与 <strong>异步 one-step-off（async）</strong>。两者的核心差别不是“是否同步权重”，而是<strong>rollout 数据和 update 步骤的时间对齐关系</strong>，即训练是on-policy或off-policy。</p> <h3 id="321-同步模式rayppotrainer-主线">3.2.1 同步模式（RayPPOTrainer 主线）</h3> <p>同步模式下，一个 step 的临界路径基本是串行：</p> <ol> <li><code class="language-plaintext highlighter-rouge">rollout</code> 生成样本；</li> <li><code class="language-plaintext highlighter-rouge">actor/critic</code> 用这批样本更新；</li> <li><code class="language-plaintext highlighter-rouge">update_weights</code> 把新参数同步到 rollout 端；</li> <li>下一步 rollout 才用新参数。</li> </ol> <p>可以抽象成： \(\text{step}_t \approx \text{gen}_t + \text{update}_t + \text{sync}_t\) 对应主线 <code class="language-plaintext highlighter-rouge">ray_trainer.py</code> 中的顺序（简化）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gen_batch_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">async_rollout_manager</span><span class="p">.</span><span class="nf">generate_sequences</span><span class="p">(</span><span class="n">gen_batch_output</span><span class="p">)</span>
<span class="bp">...</span>
<span class="n">actor_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_update_actor</span><span class="p">(</span><span class="n">batch</span><span class="p">)</span>
<span class="bp">...</span>
<span class="n">self</span><span class="p">.</span><span class="n">checkpoint_manager</span><span class="p">.</span><span class="nf">update_weights</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">global_steps</span><span class="p">)</span>
</code></pre></div></div> <p>因此，同步模式里“本步采样、下步生效”是正常现象，不是延迟故障。</p> <h3 id="322-异步模式one-step-off">3.2.2 异步模式（One-Step-Off）</h3> <p><img src="/assets/img/blog/blog2/one-step-off-pipeline.png" alt="One-Step-Off 异步流水线示意图"/> 上面一条（$r$，rollout）和下面一条（$u$，update）<strong>错位一个 step</strong>。</p> <ul> <li>rollout 在时间 $t$ 用的是 $\omega_t$；</li> <li>update 在时间 $t+1$ 消费 rollout 在 $t$ 产生的数据；</li> <li>update 完成后参数变成 $\omega_{t+1}$，再同步给下一轮 rollout。 也就是： \(\text{update}_{t+1} \leftarrow \text{rollout}_{t}\)</li> </ul> <p>这正是 one-step-off 的定义：<strong>训练数据相对当前策略滞后一步</strong>。</p> <p>在 <code class="language-plaintext highlighter-rouge">OneStepOffRayTrainer</code> 中，关键时序是：</p> <ol> <li><code class="language-plaintext highlighter-rouge">await batch_data_future</code>：取上一轮异步 rollout 结果（<code class="language-plaintext highlighter-rouge">gen</code>）；</li> <li><code class="language-plaintext highlighter-rouge">sync_rollout_weights</code>：把当前 actor 权重推到 rollout 端；</li> <li><code class="language-plaintext highlighter-rouge">create_task(_async_gen_next_batch(...))</code>：立刻启动下一轮异步生成；</li> <li>前台继续当前 batch 的 <code class="language-plaintext highlighter-rouge">compute_* + update_*</code>。</li> </ol> <p>对应代码（简化）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">with</span> <span class="nf">marked_timer</span><span class="p">(</span><span class="sh">"</span><span class="s">gen</span><span class="sh">"</span><span class="p">,</span> <span class="n">timing_raw</span><span class="p">):</span>
    <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">_</span><span class="p">,</span> <span class="n">batch</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="k">await</span> <span class="n">batch_data_future</span>   <span class="c1"># 拿到上一轮结果
</span>
<span class="k">with</span> <span class="nf">marked_timer</span><span class="p">(</span><span class="sh">"</span><span class="s">sync_rollout_weights</span><span class="sh">"</span><span class="p">,</span> <span class="n">timing_raw</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_fit_update_weights</span><span class="p">()</span>

<span class="n">batch_data_future</span> <span class="o">=</span> <span class="n">asyncio</span><span class="p">.</span><span class="nf">create_task</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">_async_gen_next_batch</span><span class="p">(...))</span>  <span class="c1"># 启动下一轮异步生成
</span>
<span class="n">batch</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_fit_compute_log_prob</span><span class="p">(</span><span class="n">batch</span><span class="p">)</span>
<span class="n">batch</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_fit_compute_advantage</span><span class="p">(</span><span class="n">batch</span><span class="p">)</span>
<span class="n">batch</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_fit_update_actor</span><span class="p">(</span><span class="n">batch</span><span class="p">)</span>
</code></pre></div></div> <p>因此在异步模式下： \(\text{step}_t \not\approx \text{gen}_t + \text{update}_t\)</p> <p>而更接近（存在重叠）： \(\text{step}_t \approx \max(\text{generate\_async}_t,\ \text{training\_only}_t) + \text{sync\_rollout\_weights}_t + \epsilon\)</p> <h3 id="323-权重不一致现象存在但可被校正">3.2.3 “权重不一致”现象存在，但可被校正</h3> <p>在 one-step-off 里，同一条轨迹在 rollout 和 update 时对应的策略参数一般不一致，也就是 rollout 用 $\pi_b$（旧），update 用 $\pi_\theta$（新），这会带来 off-policy 偏差，因此需要补偿（importance sampling / rollout correction）。</p> <h4 id="1-目标与采样分布不一致">(1) 目标与采样分布不一致</h4> <p>我们希望优化目标函数：</p> \[J(\theta)=\mathbb{E}_{\tau\sim p_\theta(\tau)}[f(\tau)]\] <p>但数据来自 $p_b(\tau)$。用测度变换可写为：</p> \[\mathbb{E}_{\tau\sim p_\theta}[f(\tau)] = \mathbb{E}_{\tau\sim p_b}\left[\frac{p_\theta(\tau)}{p_b(\tau)}f(\tau)\right]\] <p>其中，重要性权重定义为：</p> \[w(\tau)=\frac{p_\theta(\tau)}{p_b(\tau)}\] <p>这里的直觉是：从旧策略迁移到新策略后，一条轨迹是否还“值得被学习”，取决于它在新策略下出现的相对概率。比如新策略下采到该轨迹的概率趋近于 0，就不应继续根据这条轨迹更新参数。</p> <h4 id="2-在-transformer-语境下体现">(2) 在 Transformer 语境下体现</h4> <p>自回归模型下（省略与 $\theta$ 无关项）：</p> \[p_\theta(\tau)\propto \prod_t \pi_\theta(a_t|s_t),\quad p_b(\tau)\propto \prod_t \pi_b(a_t|s_t)\] <p>所以有：</p> \[\log w(\tau)=\sum_t\left[\log\pi_\theta(a_t|s_t)-\log\pi_b(a_t|s_t)\right]\] <p>进一步可得：</p> \[w(\tau)=\exp\left(\sum_t(\log\pi_\theta-\log\pi_b)\right)\] <p>这就是“通过 logprob 比率补偿”的数学来源。</p> <p>当新旧策略偏差过大时，通常会配合 <strong>PPO 的 Clip 机制</strong>来防止更新步幅过大导致训练崩溃。</p> <h3 id="324-实验量化结果异步比同步快多少硬等待有多少">3.2.4 实验量化结果：异步比同步快多少，硬等待有多少</h3> <p>基于一次总计699step的训练数据，可以做一个反事实估算：</p> <ol> <li><strong>如果改成同步实现（不再 overlap）</strong>，每步近似为： \(\text{step}_{\text{sync-est}} \approx \text{training\_only} + \text{generate\_async} + \text{sync\_rollout\_weights}\) 统计结果： <ul> <li>异步实测总时长：$62976.392\ \text{s}$</li> <li>同步估算总时长：$117517.538\ \text{s}$</li> <li>比值（同步/异步）：$1.866\times$</li> </ul> </li> </ol> <p>这意味着在当前配置下（gen和update的gpu数量1：1），one-step-off 的异步重叠把总时长压到了同步实现的大约 $53.6\%$。</p> <ol> <li><strong>异步中难以消除的等待窗口</strong>： \(\text{wait\_hard}=\sum_t\left(\text{wait\_prev\_gen}_t+\text{sync\_rollout\_weights}_t\right)\)</li> </ol> <p>其中用指标映射：</p> <ul> <li>$\text{wait_prev_gen}_t \leftrightarrow \texttt{第t步等待t-1步rollout}$</li> <li>$\text{sync_rollout_weights}_t \leftrightarrow \texttt{第t-1步rollout结束后把权重同步给vLLM}$</li> </ul> <p>统计结果：</p> <ul> <li>$\sum \texttt{timing_s/gen}=357.007\ \text{s}$（占总时长 $0.57\%$）</li> <li>$\sum \texttt{timing_s/sync_rollout_weights}=3042.036\ \text{s}$（占总时长 $4.83\%$）</li> <li>合计硬等待：$3399.043\ \text{s}$，占总时长 <strong>$5.40\%$</strong></li> </ul> <p>结论：这 $5.40\%$ 可以视为当前系统在“边界等待 + 权重切换”上的硬成本窗口；它未被完全消除，但相对于同步实现的反事实时长，异步 one-step-off 仍然带来了显著优化。</p> <h1 id="四verl-框架核心设计总结">四、Verl 框架核心设计总结</h1> <p>Verl 的核心竞争力并非仅在于其功能接口的完备性，而在于它通过对 RL 后训练（Post-training）计算负载的深度拆解，在系统架构上实现了 <strong>灵活编排与极致吞吐的平衡</strong>。其创新设计主要体现在以下三个维度：</p> <h3 id="1-混合控制架构hybrid-controller">1. 混合控制架构（Hybrid-Controller）</h3> <p>Verl 创新性地提出了 <strong>Hybrid-Controller</strong> 方案，打破了传统分布式框架在开发灵活性（Single-Controller）与执行性能（Multi-Controller）之间的平衡难题：</p> <ul> <li><strong>上层解耦编排</strong>：保留了类似单机脚本的 Single-Controller 逻辑，由主控进程（如 <code class="language-plaintext highlighter-rouge">RayPPOTrainer</code>）负责定义任务的先后顺序与数据流向，降低了算法开发门槛。</li> <li><strong>下层高效执行</strong>：在微观执行层面，Verl 将任务下发给由 Ray 管理的“子节点群”，各组内部以 Multi-Controller 模式驱动 <strong>vLLM</strong> 或 <strong>FSDP</strong> 等专业引擎，确保了硬件性能的达峰。</li> </ul> <h3 id="2-异构引擎的切换hybrid-engine">2. 异构引擎的切换（Hybrid Engine）</h3> <p>针对 RL 训练中推理（Rollout）与更新（Update）截然不同的显存与计算瓶颈，Verl 实现了模型权重的动态转换：</p> <ul> <li><strong>Rollout 阶段（带宽优化）</strong>：切换为 vLLM 模式，权重聚合为 TP 格式，利用 <strong>PagedAttention</strong> 机制消除显存碎片，将空间留给 KV Cache 以实现高并发采样。</li> <li><strong>Update 阶段（算力优化）</strong>：权重立刻重切为 FSDP 格式，通过分片存储（Sharding）压低基础显存占用，全力支持<strong>激活值驻留</strong>，从而承载超大规模参数的训练。</li> </ul> <h3 id="3-异步流水线设计one-step-off-policy">3. 异步流水线设计（One-Step-Off Policy）</h3> <p>Verl 通过 Ray 提供的异步 RPC 能力，重构了 PPO 的迭代逻辑，显著提升了集群利用率：</p> <ul> <li><strong>计算与生成的 Overlap</strong>：通过 One-Step-Off 机制，Verl 允许当前步的<strong>参数更新</strong>与下一步的<strong>数据采样</strong>在时间轴上重叠（错位执行），极大地减少了 GPU 的空转等待。</li> </ul> <h1 id="五延伸阅读与致谢">五、延伸阅读与致谢</h1> <p>在整理这篇 Blog 的过程中，我受益于以下优秀的开源项目与教程：</p> <ul> <li><strong><a href="https://verl.readthedocs.io/">verl 官方文档</a></strong>：了解框架设计哲学的第一站，关于 <strong>Hybrid-Controller</strong> 的定义非常精辟。</li> <li><strong><a href="https://zh.d2l.ai/">动手学深度学习 (D2L)</a></strong>：李沐老师的经典之作，我关于多 GPU 并行范式的理解大多源自于此。</li> </ul> <p>同时，特别感谢童雨轩学长在调研过程中提供的宝贵技术资料与思路启发~</p>]]></content><author><name></name></author><category term="work"/><category term="system"/><category term="ai"/><summary type="html"><![CDATA[从源码与实验指标出发，梳理 Verl 如何用 Hybrid-Controller 与 one-step-off 流水线连接 rollout 和 update。]]></summary></entry><entry><title type="html">当多线程遭遇 GIL：一次 CPU 密集型任务的并行化避坑指南</title><link href="https://chenyy.github.io/blog/2026/gil-cpu-parallelism/" rel="alternate" type="text/html" title="当多线程遭遇 GIL：一次 CPU 密集型任务的并行化避坑指南"/><published>2026-04-29T09:30:00+00:00</published><updated>2026-04-29T09:30:00+00:00</updated><id>https://chenyy.github.io/blog/2026/gil-cpu-parallelism</id><content type="html" xml:base="https://chenyy.github.io/blog/2026/gil-cpu-parallelism/"><![CDATA[<h1 id="前言">前言</h1> <p>昨天在实验室集群上跑一个任务，中午没多想直接让agent写了个逻辑，监控了一下正确性就退出了。到晚上看它得跑到凌晨12点，之后我还得去做质检，那不中，<del>我是早睡早起的好学生</del>。于是去询问agent是否能帮我改成并发的逻辑加快一下运行速度。它第一版改了一个基于线程的并发版本，一开始进度条还可以，后来甚至几乎完全卡住了。<del>作为AI助手的我</del>，于是把命令行内容复制给cursor让它判断问题，它很快的说了一通我似懂非懂的术语，然后建议我改成多进程并发的逻辑。<del>作为AI助手的我</del>也是立即把控制权限开放，交由它创作，大概半分钟它改完代码之后肉眼可见从几秒1条变成了1秒几条的速度。至此我遂产生了不服气感，强烈地想把这里的门道研究清楚，并且给出一个比较通用的方法论可以供大家参考。</p> <p>回顾后，这部分知识主要涉及到ICS(Introduction to Computer System)的知识。我是去年秋季学期在清华选修了《计算机系统概论》这门课，众所周知大二上是贵系生最想要升天的一个学期，我又叠了3门新雅通识课，导致当时（不得不）选择了速通这门课，只针对往年题从CSAPP（Comper Systems:A Programmer’s Perspective)的考试范围D1里自行筛选出了高频考点范围D2进行学习。以至于这部分的知识在脑子里是割裂的，甚至今天想要review的时候很多跨章节的知识需要重新开始学习。</p> <p>在gemini助手的指导下，我今天回顾了ch8的进程，ch10的描述符、文件表、v-node表，ch11的三次握手，进而去理解ch12的基于进程和线程的并发编程。现在的感觉是快要明白又有点模糊的感觉，于是想接着输出来加深一下自己的印象，也求求大佬们的指导（或者瑞平）。</p> <p>在本人生涯第一篇blog中，我准备按照以下的逻辑展开：理论基础部分，按照我的理解把CSAPP中涉及到的背景知识做一个基础的铺垫，主要涉及进程和线程的概念及调度等；实例分析部分，我会具体展开一下我昨天晚上遇到的情境，邀请你身临其境感受一下多线程vs多进程的功效；方法总结部分，如果你想要takeaway的知识直接看这部分就好！</p> <h1 id="理论基础">理论基础</h1> <h2 id="21-进程系统赋予的独占幻觉">2.1 进程：系统赋予的“独占”幻觉</h2> <p>进程本质上是内核提供给应用程序的两个核心抽象：<strong>逻辑控制流</strong>（让你觉得独占 CPU）和<strong>私有地址空间</strong>（让你觉得独占内存）。</p> <p><strong>逻辑控制流</strong>：</p> <p>如图，物理控制流被进程A，B，C切成了3份，但在进程A，B，C看来自己拥有完整的控制流，即所谓的逻辑控制流，只是自己的逻辑控制流的指令之间存在一定的时间间隔 <img src="/assets/img/blog/blog1/process-flow.jpg" alt="逻辑控制流示意图"/> <strong>私有地址空间</strong>：</p> <p>如图，对一个n位地址机器而言，每一个进程都认为自己拥有$0, 1, 2, \dots, 2^n - 1$地址用于存储变量等。一般而言，一个进程的内容无法轻易被其他进程读或写。（具体虚拟地址到物理地址的映射及读取等参见CSAPP） <img src="/assets/img/blog/blog1/address-space.jpg" alt="私有地址空间示意图"/> <strong>开发者笔记</strong>：</p> <ul> <li><strong>逻辑控制流</strong>决定了并发的<strong>响应性</strong>：虽然 CPU 在切换，但我们可以写出看起来“同时运行”的程序。</li> <li><strong>私有地址空间</strong>决定了并发的<strong>安全性</strong>：这是多进程模型最核心的资产。因为地址空间物理隔离，子进程即便因为 Bug（如模型加载失败、内存溢出）崩溃，也绝不会污染主进程的内存。 <h2 id="22-进程间的文件共享">2.2 进程间的文件共享</h2> </li> </ul> <p>在 Linux 中，“一切皆文件”。当我们说一个进程“打开了一个文件”或“建立了一个网络连接”时，内核实际上维护了三张表来管理这些资源：描述符表，打开文件表和v-node表。实际上，这三张表也是内核维护文件共享的核心处理单元。</p> <ol> <li><strong>进程级：描述符表 (Descriptor Table)</strong> <ul> <li><strong>所属</strong>：每个进程私有。</li> <li><strong>本质</strong>：一个数组，索引就是我们常说的 <code class="language-plaintext highlighter-rouge">fd</code>（0, 1, 2…）。</li> <li><strong>内容</strong>：每个条目指向全局“打开文件表”中的一个项。</li> </ul> </li> <li><strong>系统级：打开文件表 (Open File Table)</strong> <ul> <li><strong>所属</strong>：所有进程共享。</li> <li><strong>关键条目</strong>： <ul> <li><strong>文件位置 (File Offset)</strong>：当前的读写偏移量。</li> <li><strong>引用计数 (refcnt)</strong>：当前有多少个描述符指向这个条目。 <blockquote> <p>对于回收资源极其重要</p> </blockquote> </li> </ul> </li> </ul> </li> <li><strong>系统级：v-node 表 (v-node Table)</strong> <ul> <li><strong>所属</strong>：所有进程共享。</li> <li><strong>内容</strong>：包含文件的静态信息（如磁盘位置、文件大小、类型等）。</li> </ul> </li> </ol> <p>以下举3个例子简单描述一下3张表的逻辑关系：</p> <ul> <li> <p>一个进程打开2份不同文件时： <img src="/assets/img/blog/blog1/fd-open-two-files.jpg" alt="一个进程打开两份不同文件"/></p> </li> <li>一个进程通过open()两次打开同一个文件时： <img src="/assets/img/blog/blog1/fd-open-same-file.jpg" alt="一个进程两次打开同一文件"/></li> <li>一个进程通过fork产生一个子进程后，父子进程对文件的管理： <img src="/assets/img/blog/blog1/fork-fd-sharing.jpg" alt="fork 后父子进程的文件管理"/> <strong>开发者笔记</strong>：</li> <li><strong>理解引用计数<code class="language-plaintext highlighter-rouge">refcnt</code></strong>：这是理解“优雅关闭”的关键。如果子进程不 <code class="language-plaintext highlighter-rouge">close</code> 掉继承来的监听描述符，或者父进程不 <code class="language-plaintext highlighter-rouge">close</code> 掉已分发的连接描述符，内核就不会真正回收资源，最终会导致“文件描述符耗尽”报错。</li> <li><strong>共享 <code class="language-plaintext highlighter-rouge">File Offset</code> 的风险</strong>：这是多进程并发中最隐蔽的坑。因为父子进程共享同一个偏移量，如果多个进程同时向一个文件 <code class="language-plaintext highlighter-rouge">write</code>，数据会发生交织。 <h2 id="23-基于进程的并发原理父子进程的描述符分发">2.3 基于进程的并发原理：父子进程的描述符分发</h2> </li> </ul> <p>下面展示了一次连接的完整生命周期：从内核描述符的角度解释了父进程是如何调度子进程完成多进程并发的。注意，此处CSAPP中提及的仅为最简化的版本，即在任务分发时已明确需要执行的任务等，实际过程中父子进程需要在分发后不断通信进行任务的分发与交付。（关于父子进程的更多基础知识见CSAPP）</p> <p>为方便理解，以下用形象化的语言，把需要执行的任务定义为“客户端“，而将分发多进程的父进程定义为”服务器“。</p> <p>2.3.1 服务器初始化</p> <ul> <li><strong>创建 Socket</strong>：服务器进程调用 <code class="language-plaintext highlighter-rouge">socket()</code> 系统调用。内核在进程的<strong>描述符表</strong>中分配一个最小的可用索引（假设为 <code class="language-plaintext highlighter-rouge">fd 3</code>），并在内核空间创建一个对应的套接字结构。</li> <li><strong>绑定与配置</strong>：通过 <code class="language-plaintext highlighter-rouge">bind()</code> 将 <code class="language-plaintext highlighter-rouge">fd 3</code> 与特定的 IP 地址和端口号关联。</li> <li><strong>转换为监听状态</strong>：服务器调用 <code class="language-plaintext highlighter-rouge">listen(3, ...)</code>。此时，<code class="language-plaintext highlighter-rouge">fd 3</code> 的性质发生变化，成为<strong>监听描述符 (Listening Descriptor)</strong>。它不再负责数据传输，而是专门负责处理内核中 TCP 全连接队列里的连接请求。</li> </ul> <p>2.3.2 客户端发起连接</p> <ul> <li><strong>客户端初始化</strong>：客户端进程调用 <code class="language-plaintext highlighter-rouge">socket()</code> 创建自己的描述符（假设为 <code class="language-plaintext highlighter-rouge">fd 5</code>）。</li> <li><strong>发起握手</strong>：客户端调用 <code class="language-plaintext highlighter-rouge">connect(5, server_addr, ...)</code>，内核协议栈向服务器发送 <strong>SYN</strong> 包（第一次握手）。</li> <li><strong>协议栈处理</strong>：服务器内核在接收到 SYN 并完成 <strong>SYN-ACK</strong> 与 <strong>ACK</strong>（三次握手）后，将该连接从“半连接队列”移动到“已连接队列”。</li> </ul> <p>2.3.3 连接的建立与分发</p> <ul> <li><strong>描述符生成</strong>：服务器主进程调用 <code class="language-plaintext highlighter-rouge">accept(3, ...)</code>。该调用从已连接队列中取出一个条目，内核为此连接在描述符表中分配一个新的空位（假设为 <code class="language-plaintext highlighter-rouge">fd 4</code>）。</li> <li><strong>职责分离</strong>： <ul> <li><strong>fd 3 (监听描述符)</strong>：继续保持监听状态，负责后续其他客户端的握手请求。</li> <li><strong>fd 4 (已连接描述符)</strong>：代表一个具体的客户端 TCP 链接，负责该连接的数据读写。</li> </ul> </li> <li><strong>创建子进程</strong>：为了实现并发，服务器主进程调用 <code class="language-plaintext highlighter-rouge">fork()</code>。</li> </ul> <p>2.3.4 并发处理中的描述符共享</p> <ul> <li><strong>副本生成</strong>：根据 <code class="language-plaintext highlighter-rouge">fork</code> 机制，子进程获得父进程描述符表的<strong>完整副本</strong>。此时，子进程的描述符表中同样存在指向同一个内核文件表项的 <code class="language-plaintext highlighter-rouge">fd 3</code> 和 <code class="language-plaintext highlighter-rouge">fd 4</code>。</li> <li><strong>关闭冗余句柄</strong>： <ul> <li><strong>子进程行为</strong>：立即执行 <code class="language-plaintext highlighter-rouge">close(3)</code>。因为它只负责处理当前连接，不需要监听新请求。它通过 <code class="language-plaintext highlighter-rouge">fd 4</code> 与客户端进行 I/O 交互。</li> <li><strong>父进程行为</strong>：立即执行 <code class="language-plaintext highlighter-rouge">close(4)</code>。因为它已将该连接的控制权“交给”了子进程，它需要腾出资源去继续监听 <code class="language-plaintext highlighter-rouge">fd 3</code>。</li> </ul> </li> <li><strong>引用计数管理</strong>：内核中对应 <code class="language-plaintext highlighter-rouge">fd 4</code> 的<strong>打开文件表项</strong>的引用计数（<code class="language-plaintext highlighter-rouge">refcnt</code>）最初为 2。只有当父子进程都执行了 <code class="language-plaintext highlighter-rouge">close</code> 后，<code class="language-plaintext highlighter-rouge">refcnt</code> 归零，该物理连接才会真正被销毁。</li> </ul> <p><img src="/assets/img/blog/blog1/accept-lifecycle.jpg" alt="连接生命周期示意图"/></p> <p>2.3.5 父进程调度不同客户端请求</p> <p>此处父进程相当于调度中心，通过将不同的描述符分发给不同的子进程分别处理不同的客户端需求。 <img src="/assets/img/blog/blog1/parent-dispatch.jpg" alt="父进程分发不同客户端请求"/></p> <p><strong>开发者笔记</strong>：在这个模型中，父进程通过描述符分发任务，本质上是“任务状态与执行环境的完全剥离”。父进程只管“描述符/任务包”，子进程在封闭的环境里“处理计算”。这种架构天然支持多核并行，因为每个“进程”都是独立的，这为我们后面解决 Python 的 GIL 问题提供了理论基础。</p> <h2 id="24-线程轻量级的逻辑流与共享">2.4 线程：轻量级的逻辑流与共享</h2> <p>如果说进程是操作系统分配资源的最小单位，那么线程就是 CPU 调度的最小单位。</p> <p>2.4.1 线程的上下文</p> <p>与进程拥有庞大的私有地址空间不同，线程非常轻量，只拥有维持运行所必须的极少量私有数据：</p> <ul> <li><strong>线程 ID (TID)</strong>：每个线程的唯一标识。</li> <li><strong>程序计数器 (PC)</strong>：记录当前执行到的指令位置。</li> <li><strong>寄存器与栈指针</strong>：存储临时变量和函数调用记录。</li> </ul> <p>2.4.2 共享、透明的内存空间</p> <p>在同一个进程内的多个线程，就像是在同一个办公室里共用资料库的员工。它们共享：</p> <ul> <li><strong>整个虚拟地址空间</strong>：包括只读的代码段、读/写数据段、以及堆。这意味着一个线程创建的对象，其他线程可以直接读写。</li> <li> <p><strong>打开的文件集合</strong>：所有的文件描述符对所有线程是完全透明共享的。</p> </li> <li><strong>开发者笔记</strong>： <ul> <li><strong>低开销及代价</strong>：线程的创建比进程快得多，因为不需要复制页表（进程的私有地址空间在触发写操作时需要通过复制页表实现，但如果只是读操作会触发COW机制延迟复制）。但这种“零距离”共享意味着<strong>一个线程写坏了内存，整个进程都会挂掉</strong>。</li> <li><strong>通信零成本</strong>：线程间通信不需要像多进程那样搞 IPC（管道/队列），直接读写全局变量即可。但也正是这种便利，我们需要花费更多的精力和运行时间去安排“竞争” <h2 id="25-变量映射与同步风险">2.5 变量映射与同步风险</h2> </li> </ul> </li> </ul> <p>由于线程间内存是共用的，资源的管理变得极其复杂。</p> <ul> <li><strong>竞争条件</strong>：由于多线程可以同时访问相同内存，即使是 <code class="language-plaintext highlighter-rouge">count += 1</code> 这样简单的操作，在 CPU 层面也涉及“读-改-写”三步。如果没有同步，两个线程可能同时读到旧值，导致更新丢失。</li> <li> <p><strong>同步元语</strong>：为了解决竞争，必须引入<strong>互斥锁</strong>。（关于互斥锁更详细的内容参见CSAPP信号量一节）</p> </li> <li><strong>开发者笔记</strong>： <ul> <li><strong>锁争用</strong>：在一个进程内开启多个线程时，如果它们都在抢同一把锁，多个线程会处于阻塞状态。并行的代码实际上退化成了<strong>串行</strong>。</li> <li><strong>不可见的性能税</strong>：加锁、解锁、以及内核在不同线程间疯狂切换，会消耗大量 CPU 时钟周期。</li> </ul> </li> </ul> <h2 id="26-python-的特殊枷锁gil-global-interpreter-lock">2.6 Python 的特殊枷锁：GIL (Global Interpreter Lock)</h2> <p>这把GIL锁就是2.5中提到的互斥锁，也是我在实验中遇到的耗时飙升的底层根源：</p> <ul> <li><strong>核心限制</strong>：为了内存安全，CPython 解释器要求在任何时刻，一个进程中<strong>只有一个线程</strong>能持有 GIL 执行 Python 字节码。</li> <li><strong>CPU 密集型困境</strong>：在进行计算（涉及大量正则匹配、HTML 解析）时，线程会死死攥住 GIL 不放。</li> <li> <p><strong>性能退化</strong>：多个线程抢夺唯一的 GIL，产生巨大的上下文切换开销。这种“伪并行”不仅无法利用多核 CPU，反而因为内耗导致单位样本处理速度显著退化。</p> </li> <li><strong>开发者笔记</strong>： <ul> <li><strong>识别任务类型</strong>：这是方法论的核心。如果你的任务是 <code class="language-plaintext highlighter-rouge">time.sleep()</code> 或等待网络 API 返回（I/O 密集），多线程是神药；如果是疯狂算逻辑的任务（CPU 密集），<strong>多线程就是毒药</strong></li> </ul> </li> </ul> <h1 id="实例分析从几乎卡死到秒速处理">实例分析：从“几乎卡死”到“秒速处理”</h1> <p>在昨天的场景中，优化的核心任务大致可以理解为：对2w条模型输出进行归一化并根据模型输出和gt计算某种得分。这是一个典型的计算密集型（CPU-bound）任务，对 CPU 算力有着极高的连续需求。</p> <h2 id="演进路线三种计算模型的客观实验">演进路线：三种计算模型的客观实验</h2> <p>在尝试中我先后尝试了以下三种方法，分别对应不同的系统级处理逻辑与资源分配方式。</p> <h3 id="阶段-a串行流">阶段 A：串行流</h3> <ul> <li><strong>运行机制</strong>：采用标准 <code class="language-plaintext highlighter-rouge">for row in rows</code> 循环。程序在单一逻辑控制流下运行，指令按序进入 CPU。</li> <li><strong>资源利用</strong>：仅占用 1 个 CPU 核心，其余 15 核处于“围观”状态。</li> <li><strong>实际表现</strong>： <ul> <li><strong>平均耗时</strong>：<strong>1.76s</strong> / 样本。</li> <li><strong>评价</strong>：逻辑最简单，但面对近 2 万条数据，预估总耗时超过 8 小时，无法满足快速迭代的需求。</li> </ul> </li> </ul> <h3 id="阶段-b多线程">阶段 B：多线程</h3> <ul> <li><strong>尝试</strong>：使用 <code class="language-plaintext highlighter-rouge">ThreadPoolExecutor</code>，试图通过 64 个并发线程来切分任务，并为每个线程准备了私有的计算实例用来计算gt和模型回复之间的匹配程度。 <blockquote> <p>这里我当时天真的以为max_workers开的越大就应该越快吧 [🤦]</p> </blockquote> </li> <li><strong>核心矛盾（GIL 枷锁）</strong>： <ul> <li>计算涉及大量的正则匹配与矩阵运算，属于 Python 字节码密集操作，无法触发 GIL 的自动释放。</li> <li>16 个物理核心上的 64 个线程在内核中疯狂争夺唯一的 <strong>GIL 锁</strong>。</li> </ul> </li> <li><strong>实际表现</strong>： <ul> <li><strong>平均耗时</strong>：<strong>96.1926s</strong> / 样本。</li> <li><strong>现象</strong>：性能发生严重<strong>退化</strong>，单条耗时相比串行反而慢了约 200 倍。系统 99% 的能量消耗在了“为了抢锁而进行的无效上下文切换”上。</li> <li><strong>分析</strong>：在 CPU 密集任务中，由于线程无法主动交出锁，OS 会频繁强制切换线程。每一次切换都要保存/恢复寄存器、刷新缓存，这在 64 个线程的高并发下会产生“惊群效应”。即多线程阶段的高耗时并非 CPU 算力不足，而是 99% 的时间都浪费在了 <strong>L3 Cache 的频繁失效</strong>和内核态/用户态的无意义切换上。</li> </ul> </li> </ul> <h3 id="阶段-c多进程真正释放多核福利">阶段 C：多进程——真正释放多核福利</h3> <ul> <li><strong>策略</strong>：切换为 <code class="language-plaintext highlighter-rouge">ProcessPoolExecutor</code>，开启 16 个独立的 worker 进程。</li> <li><strong>技术优势（内核级对齐）</strong>： <ul> <li><strong>绕过 GIL</strong>：每个进程拥有独立的 Python 解释器和 GIL，16 个核心真正实现了<strong>真并行</strong>。</li> <li><strong>内存优化</strong>：利用 <code class="language-plaintext highlighter-rouge">initializer</code> 进行进程级一次性初始化，并配合 <strong>COW (写时复制)</strong> 机制，确保了高效的资源利用。</li> <li><strong>I/O 收口</strong>：针对 <strong>File Offset 共享</strong> 风险，设计了“子进程只传变量，主进程统一写盘”的模式，规避了并发写导致的文件损坏。</li> </ul> </li> <li><strong>实际表现</strong>： <ul> <li><strong>平均耗时</strong>：<strong>2.21s</strong> / 样本。</li> <li><strong>吞吐量</strong>：内核同时启动16个进程，吞吐量约<strong>0.14s</strong> / 样本，吞吐量显著提升，耗时分布恢复正常，整体处理速度相比串行提升了约 10-12 倍。</li> <li><strong>进程间通信</strong>：多进程的单条耗时略增（1.7s -&gt; 2.2s），是由于主进程需要将数据 <strong>Pickle 序列化</strong>后通过管道（Pipe）传输给子进程。（相比于ICS课本中提到的父进程接收到信号即可分发好对应数据的模型，现实使用中更多是需要父进程动态分发数据给不同的子进程去处理，之后子进程将处理好的数据返还给父进程）</li> </ul> </li> </ul> <h1 id="方法总结多线程还是多进程">方法总结：多线程还是多进程？</h1> <p>通过以上的分析，我们可以总结出一套在 Python 环境下选择并发模型的通用方法论。</p> <h2 id="31-核心判断矩阵">3.1 核心判断矩阵</h2> <p>选择并发模型的第一步，是识别任务的“性能瓶颈”在哪里：</p> <table> <thead> <tr> <th><strong>维度</strong></th> <th><strong>多线程</strong></th> <th><strong>多进程</strong></th> </tr> </thead> <tbody> <tr> <td><strong>瓶颈定位</strong></td> <td><strong>I/O 密集型</strong></td> <td><strong>CPU 密集型</strong></td> </tr> <tr> <td><strong>典型场景</strong></td> <td>爬虫、API 请求、数据库读写、文件下载</td> <td><strong>大模型计算</strong>、图像处理、复杂正则匹配</td> </tr> <tr> <td><strong>通信成本</strong></td> <td><strong>极低</strong>（共享进程内存，直接读写变量）</td> <td><strong>较高</strong>（需跨进程序列化，通过 Queue/Pipe 通信）</td> </tr> <tr> <td><strong>安全性</strong></td> <td><strong>较低</strong>（一者崩则全家崩；存在竞争风险）</td> <td><strong>极高</strong>（物理隔离，子进程崩溃不影响主进程）</td> </tr> <tr> <td><strong>Python 限制</strong></td> <td><strong>受限于单 GIL</strong>（16 核也只能跑 1 核速度）</td> <td><strong>绕过 GIL</strong>（真正实现多核并行）</td> </tr> </tbody> </table> <h2 id="32-决策算法三步走指南">3.2 决策算法：三步走指南</h2> <p>当你面对一个需要加速的任务时，请按以下顺序自问：</p> <h3 id="第一步代码里有没有等的操作">第一步：代码里有没有“等”的操作？</h3> <ul> <li>如果在执行过程中，CPU 大部分时间在等待网络回包、磁盘 I/O 或 <code class="language-plaintext highlighter-rouge">time.sleep()</code>，请首选 <strong>多线程</strong>。</li> <li><strong>理由</strong>：线程切换极快，且在等待时会自动释放 GIL，让出执行权。</li> </ul> <h3 id="第二步逻辑里有没有算的操作">第二步：逻辑里有没有“算”的操作？</h3> <ul> <li>如果代码在疯狂进行循环运算、矩阵解析或大量文本处理，请务必选择<strong>多进程</strong>。</li> <li><strong>理由</strong>：计算任务不会主动交出 GIL。在单 GIL 下加线程，只会导致 2.5 节所说的“指令级冲突”和“锁争用”。</li> </ul> <h3 id="第三步数据是否需要写回">第三步：数据是否需要“写回”？</h3> <ul> <li>如果多个并发单元需要修改同一个全局字典或写入同一个文件： <ul> <li><strong>多线程</strong>：必须加 <strong>Mutex（互斥锁）</strong>，但这会进一步拖慢 CPU 密集任务。</li> <li><strong>多进程</strong>：推荐采用 <strong>“分布式计算 + 主进程聚合”</strong> 模式。让子进程只管计算并返回结果，由主进程负责唯一的 I/O 操作。</li> </ul> </li> </ul> <h2 id="33-避坑避雷指南">3.3 避坑避雷指南</h2> <ol> <li> <p><strong>不要迷信“线程轻量”</strong>： 在 Python 里，线程确实比进程省内存，但在 CPU 密集任务面前，多出的那点内存开销远比不上 200 倍的性能退化（如本次实验中的 99s 惨案）。</p> </li> <li> <p><strong>利用 COW (写时复制) 优化进程初始化</strong>： 在面对计算实例这种大型对象时，利用 <code class="language-plaintext highlighter-rouge">multiprocessing</code> 的 <code class="language-plaintext highlighter-rouge">initializer</code> 在 <code class="language-plaintext highlighter-rouge">fork</code> 瞬间进行“逻辑共享、物理隔离”的加载，是兼顾内存与速度的最优解。</p> </li> <li> <p><strong>警惕文件偏移量共享</strong>： 牢记 2.2 节的理论：<strong>文件位置是系统级共享的</strong>。始终坚持由主进程单点写盘，或为每个子进程分配独立的临时文件。</p> </li> </ol>]]></content><author><name></name></author><category term="work"/><category term="system"/><summary type="html"><![CDATA[从串行到多线程再到多进程，复盘一次 CPU 密集型任务并发优化中的关键坑点。]]></summary></entry></feed>